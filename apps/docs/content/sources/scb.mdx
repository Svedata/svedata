---
title: SCB
description: Statistiska centralbyråns statistikdatabas via PxWebAPI 2.0
---

# SCB

Statistiska centralbyråns (SCB) statistikdatabas via REST-API:t
PxWebAPI 2.0 på `statistikdatabasen.scb.se/api/v2/`. Ingen API-nyckel.
Rate-limit: max 30 anrop per 10 sekunder och högst 150 000 dataceller
per anrop.

## Metoder

### `svedata.scb.search(query, options?)`

Sök tabeller i statistikdatabasen. Returnerar tabellsammanfattningar
inklusive `id`, `label`, `first_period`, `last_period` och
`variable_names`.

```ts
// Default lang är 'sv' — sök på svenska
const { data } = await svedata.scb.search('befolkning', { pageSize: 10 });
// data.total = 173
// data.tables[0].label = 'Befolkningens anslutning till ...'

// Engelska sökord kräver lang: 'en'
const en = await svedata.scb.search('Population by region', {
  lang: 'en',
  pageSize: 10,
});
// en.data.total = 315
```

**SCB söker mot tabellernas labels i det språk man frågar.** En svensk
sökterm med `lang: 'en'` ger 0 träffar och vice versa. Default är `'sv'`
eftersom Svedata primärt används mot svensk data — sätt `lang: 'en'`
explicit för engelska sökord.

`options.lang`: `'sv'` (default) eller `'en'`.
`options.page`: sidnummer (default 1).
`options.pageSize`: max-resultat per sida (default 20).

### `svedata.scb.table(tableId, options?)`

Hämta full metadata för en tabell, inklusive dess plats i ämnesträdet.

```ts
const { data } = await svedata.scb.table('TAB638');
// data.label = 'Population by region, marital status, age and sex...'
// data.paths = [[{ id: 'BE', label: 'Population' }, ...]]
```

### `svedata.scb.data(tableId, options?)`

Hämta data från en tabell. Returneras som JSON-Stat 2.0 i `jsonstat`-fältet,
plus metadata på topnivå.

```ts
const { data } = await svedata.scb.data('TAB638', { lang: 'sv' });
// data.table_id = 'TAB638'
// data.label, data.updated, data.source
// data.jsonstat — hela JSON-Stat 2.0-objektet (dimension, value, ...)
```

För komplicerade urval (filtrera regioner, år, etc.) hämta tabellen
direkt med en HTTP-klient mot PxWebAPI 2.0 i v0.1 — flat-query-stöd
kommer i en senare version.

## Language and search results

SCB:s `lang`-parameter påverkar **både presentation och söklingvistik**.
Den är inte bara en visningsswitch — den väljer också vilket korpus
sökningen körs mot.

**Träffmängden skiljer sig mellan språk** eftersom SCB:s metadata-
täckning varierar. Engelska labels finns på fler tabeller än svenska,
och vissa termer är semantiskt bredare i ett språk än i ett annat:

| Anrop | Träffar |
|---|---|
| `search('befolkning')` (default `lang: 'sv'`) | ~173 |
| `search('population', { lang: 'en' })` | ~315 |

Det är inte en bug i SDK:n — det är vad SCB returnerar. "Population"
är ett bredare paraplybegrepp på engelska och täcker tabeller från
fler ämnesområden än det snävare svenska "befolkning". Engelska
metadata finns dessutom på fler tabeller totalt sett.

**Rekommendation:**

- Använd det språk som matchar din målgrupp och din data. Svensk för
  svenska use-cases (myndighetsrapporter, lokala dashboards). Engelska
  för internationella jämförelser eller publikationer.
- Kör söket på båda språken om du behöver maximal täckning — det är
  två separata anrop, ingen automatisk union.

**Sökning returnerar metadata, inte data.** `search()` ger en lista
av tabellsammanfattningar (`id`, `label`, `first_period`, ...). Hämta
faktisk data genom att skicka ett tabell-id till `svedata.scb.data(id)`.

## Felhantering

- 404 / okänd tabell → `{ data: null, meta }`
- 429 (rate-limit) → `{ data: null, meta: { rate_limit_remaining: 0 } }`
- Inga exceptions
